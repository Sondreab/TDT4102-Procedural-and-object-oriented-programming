#include "cannonball.h"



int main() {
	/*Test 1
	std::cout << velY(25.0, 2.5) << "	" << velY(25.0, 5.0) << std::endl;
	std::cout << posX(50.0, 2.5) << "	" << posX(50.0, 5.0) << std::endl;
	std::cout << posY(25.0, 2.5) << "	" << posY(25.0, 5.0) << std::endl;
	

	Test 2
	double theta;
	double absVelocity;
	double velocityX;
	double velocityY;
	getUserInput(&theta, &absVelocity);
	std::cout << "Theta: " << theta << "\nFart: " << absVelocity << std::endl;
	getVelocityVector(theta, absVelocity, &velocityX, &velocityY);
	std::cout << "Fart i X-retning: " << velocityX
		<< "\nFart i Y-retning: " << velocityY << std::endl;
	

	test 3
	std::srand(std::time(NULL));
	for (int i = 0; i < 30; i++) {
		std::cout << randomWithLimits(1, 10) << std::endl;
	}
	
	
	test 4
	std::random_device seeder;
	std::default_random_engine generator(seeder());

	for (int i = 0; i < 30; i++) {
		std::cout << modernRandomWithLimits(1, 3, generator) << std::endl;
	}
	*/
	playTargetPractice();
	return 0;
}